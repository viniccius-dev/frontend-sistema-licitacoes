import { Container } from './styles';

import { Search } from '../Search';

export function Biddings() {
    return (
        <Container>
            <Search filter="all"/>
        </Container>
    );
}